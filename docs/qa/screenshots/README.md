# Generated by `npm run qa:screenshots`. Commit updates with checklist changes.

Re-baselined 2026-09-25 after the Highland mesh import, then again the same day for the decklid TRUNK leader and interior materials. `parked-home.png` is the full parked capture. `parked-cabin.png` is a glasshouse crop of that capture so the cabin colors are readable. Parked-home is scored in `docs/qa/MESH_REVIEW.md` against `nata-parked-car-vis.jpg`. Proportions PASS. Paint, glass, wheels, lighting, and leaders stay PARTIAL. FSD viz to map polyline stays PASS.

